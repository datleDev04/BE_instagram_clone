class genderController {
    static createNewGender = (err, req, res, next) => {
        try {
            
        } catch (error) {
            next(error)
        }
    }
}

export default genderController